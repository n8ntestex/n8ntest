name: CI (Ubuntu only)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest  # yalnızca Ubuntu runner
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sistem bilgisi
        run: |
          uname -a
          lsb_release -a || cat /etc/os-release
